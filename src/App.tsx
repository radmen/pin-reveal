export function App(): preact.JSX.Element {
  return (
    <main class="app-shell">
      <section class="hero" aria-labelledby="app-title">
        <p class="eyebrow">Pin Reveal</p>
        <h1 id="app-title">Reveal what matters, one pin at a time.</h1>
        <p class="hero-copy">
          A minimal Preact app shell ready for focused product work.
        </p>
      </section>
    </main>
  );
}
